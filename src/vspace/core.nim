############################################################################################################################
######################################################## CRUISE COLORS #####################################################
############################################################################################################################

type
  CRGB = concept c
    compile(c.r)
    compile(c.g)
    compile(c.b)

  CRGBA = concept c
    compile(c.r)
    compile(c.g)
    compile(c.b)
    compile(c.a)

    